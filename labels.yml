## used for defining the labels to put on PR on various events

allLabels:
  - paths:
     - cmd/
    label:
      - cli
  - paths:
      - pkg/
    label:
      - operator
  - paths:
      - test/
    label:
      - bdd test
default:
  - needs review